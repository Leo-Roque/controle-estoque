# Nome da aplica��o
spring.application.name=controle-estoque

# Configura��es de Conex�o com o Banco de Dados
spring.datasource.url=jdbc:mysql://localhost:3306/controle_estoque_db
spring.datasource.username=root
spring.datasource.password=1967

# Configura��es do JPA e Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Porta do servidor (caso deseje alterar)
# server.port=8080

# Configura��es do Swagger (opcional, para documenta��o da API)
springdoc.swagger-ui.path=/swagger-ui.html
